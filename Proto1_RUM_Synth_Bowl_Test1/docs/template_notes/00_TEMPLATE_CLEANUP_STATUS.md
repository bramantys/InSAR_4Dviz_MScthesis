1, config ## Config status

Created initial config file:

`config/project_config.json`

This config is currently documentation/reference only. Existing scripts and viewer still use hardcoded paths.

Next goal: gradually update scripts to read from this config, one script at a time.